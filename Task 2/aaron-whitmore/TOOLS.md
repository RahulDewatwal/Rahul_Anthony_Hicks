# Tools: Aaron Whitmore

## Tool Usage

### Connected Services

#### Email, Messaging & Notifications
- **Gmail** (`gmail-api`): Aaron's account is `aaron.whitmore@voissync.ai`. Vendor receipts, vet paperwork, and school notices land here. Jenny checks it more than Aaron does.
- **WhatsApp** (`whatsapp-api`): Group thread with Carl Perkins and a few neighbors for quick roundup pings and "you around?" check-ins.
- **Telegram** (`telegram-api`): Not personally used. Bobby forwards family photos that Jenny reposts. Read-only awareness.
- **Discord** (`discord-api`): Wyatt's friends use it for Roblox chat. Aaron stays out of it. Read-only awareness only.
- **Microsoft Teams** (`microsoft-teams-api`): Not in Aaron's life. Background only, surfaces if an outside contractor schedules through it.
- **Outlook** (`outlook-api`): Not personally used. Some equipment vendors send from Outlook addresses; Aaron reads through Gmail.
- **Slack** (`slack-api`): Background. Briscoe does not run on Slack. Aaron has never logged in.
- **Zoom** (`zoom-api`): Used for the rare doctor follow-up with Dr. Chen and occasional pastoral check-ins with Pastor Dan.
- **Twilio** (`twilio-api`): Powers SMS reminders to Aaron's iPhone 12 for medication refills and Wednesday Little League practice.
- **SendGrid** (`sendgrid-api`): Background transactional sender for receipts and confirmations Aaron receives from vendors.
- **Mailgun** (`mailgun-api`): Same role as SendGrid. Background sender for vendor notices.
- **Intercom** (`intercom-api`): Background. Surfaces in vendor support widgets when Aaron messages Tractor Supply or HughesNet online.
- **Zendesk** (`zendesk-api`): Background. Read-only access to ticket history with HughesNet and a couple of parts suppliers.
- **Freshdesk** (`freshdesk-api`): Background. Same role as Zendesk for the equipment dealer's support portal.

#### Calendar, Documents & Storage
- **Google Calendar** (`google-calendar-api`): Jenny maintains the family calendar. Aaron reads it through you for practice, doctor visits, and school events.
- **Google Classroom** (`google-classroom-api`): Read-only into Wyatt and Emma's Silverton Elementary assignments. Surface anything due that needs prep.
- **Google Drive** (`google-drive-api`): Family docs, ranch records, scanned vet paperwork. Jenny organizes; Aaron retrieves by request.
- **Dropbox** (`dropbox-api`): Backup of equipment photos Aaron takes for parts lookups. Read and write.
- **Box** (`box-api`): Not personally used. The county once routed property paperwork through a Box link. Read-only awareness.
- **Notion** (`notion-api`): Jenny tried it for ranch bookkeeping and abandoned it for QuickBooks. Read-only legacy notes remain.
- **Obsidian** (`obsidian-api`): Not used. Aaron writes in spiral notebooks. Background only.
- **Airtable** (`airtable-api`): Not used personally. Background; surfaces if a vet clinic sends a form.
- **DocuSign** (`docusign-api`): Equipment purchase and lease paperwork. Aaron signs through this when a dealer sends a contract.
- **Calendly** (`calendly-api`): Pastor Dan uses it for member visits. Aaron books occasional pastoral check-ins through it.
- **Typeform** (`typeform-api`): Read-only. School and church surveys come through here. Jenny usually fills them out.
- **Figma** (`figma-api`): Not used. Background only, surfaces if a marketing contact sends a design link.

#### Weather, Maps & Local Discovery
- **OpenWeather** (`openweather-api`): Critical. Aaron checks before every workday and during calving. Flag pressure drops as a known migraine trigger.
- **Google Maps** (`google-maps-api`): Distance and routing to Amarillo, Clarendon, and McLean. Used heavily for parts pickup runs.
- **NASA** (`nasa-api`): Satellite imagery and drought monitoring for the 160 acres and grazing rotation planning.
- **Yelp** (`yelp-api`): Hours and ratings for DQ Silverton, Red River Steakhouse, and Maria's in Clarendon. Quick lookup only.

#### Parts, Shipping & Marketplaces
- **Amazon Seller** (`amazon-seller-api`): Background. Aaron does not sell on Amazon. Buyer-side ordering happens through Jenny's standard Amazon account, which is not connected.
- **Etsy** (`etsy-api`): Jenny browses for handmade items. Aaron uses it about once a year for her birthday.
- **Pinterest** (`pinterest-api`): Jenny saves recipes and Emma's birthday party ideas. Aaron has no account but reads what Jenny shares.
- **Shippo** (`shippo-api`): Powers shipping labels when Aaron sends a tool out to be rebuilt. Background for tracking.
- **FedEx** (`fedex-api`): Tracking parts shipments to Briscoe from Amarillo and out of state.
- **UPS** (`ups-api`): Same role as FedEx. Most ranch parts arrive by UPS Ground.
- **BigCommerce** (`bigcommerce-api`): Background. Some farm-and-ranch storefronts run on it; surfaces in price lookups.
- **WooCommerce** (`woocommerce-api`): Background. Some specialty parts suppliers run on it.

#### Ranch Books, Finance & Property
- **QuickBooks** (`quickbooks-api`): Jenny runs ranch bookkeeping here. Aaron reads year-end summaries only.
- **Xero** (`xero-api`): Background alternative to QuickBooks. Not used personally.
- **Plaid** (`plaid-api`): Not connected to First National Bank of Briscoe. Read-only awareness only.
- **Stripe** (`stripe-api`): Background. Some online vendors Aaron buys from process payments through it.
- **Square** (`square-api`): Background. A couple of feed stores in Clarendon take card through Square.
- **PayPal** (`paypal-api`): Occasionally used for an online parts vendor that requires it.
- **Alpaca** (`alpaca-api`): Not used. Aaron holds no brokerage account. Background only.
- **Coinbase** (`coinbase-api`): Not used. Read-only awareness.
- **Binance** (`binance-api`): Not used. Background only.
- **Kraken** (`kraken-api`): Not used. Background only.
- **Zillow** (`zillow-api`): Aaron quietly watches land values around Briscoe and the neighboring counties.

#### Home, Family & Faith
- **Ring** (`ring-api`): Front porch camera. Aaron checks at night when Grit barks. Mute notifications during calving naps.
- **Instagram** (`instagram-api`): Jenny's account, read-only awareness for context. Aaron avoids it.

#### Health, Fitness & Logging
- **MyFitnessPal** (`myfitnesspal-api`): Not used. Aaron's exercise is ten hours of ranch work, not a logged routine.
- **Strava** (`strava-api`): Not used. Aaron does not record his miles. Background only.

#### Media, Reading & Downtime
- **Spotify** (`spotify-api`): Not used. Aaron plays FM radio in the truck and the shop. Background only.
- **YouTube** (`youtube-api`): Tractor repair walkthroughs, John Deere service videos, and the kids' cartoons on the iPad.
- **Twitch** (`twitch-api`): Not used. Background only. Wyatt's friends watch it.
- **Vimeo** (`vimeo-api`): Not used. Background only.
- **TMDB** (`tmdb-api`): Read-only lookups for Netflix and Disney+ titles when Jenny asks what something is rated.
- **Reddit** (`reddit-api`): Occasional read-only checks of r/Ranching and r/JohnDeere for repair tips. No account.
- **Twitter** (`twitter-api`): Read-only for the National Weather Service Amarillo feed and local severe-weather alerts.
- **LinkedIn** (`linkedin-api`): Read-only. Aaron has no account, but checks on Bobby's career updates at the Amarillo dealership.
- **OpenLibrary** (`openlibrary-api`): Lookups for Louis L'Amour westerns and equipment manuals Aaron wants to track down.

#### Travel, Outings & Events
- **Uber** (`uber-api`): Not available in Briscoe. Used only when Aaron has to fly out of Amarillo for a rare trip.
- **DoorDash** (`doordash-api`): Not available locally. Background only.
- **Instacart** (`instacart-api`): Not available locally. Jenny shops in person at United in Clarendon.
- **Airbnb** (`airbnb-api`): Read-only. Used once a year if Jenny floats a long-weekend idea Aaron will probably decline.
- **Amadeus** (`amadeus-api`): Background travel search. Aaron has not flown in five years.
- **Eventbrite** (`eventbrite-api`): Lookups for the Tri-State Fair and local livestock events.
- **Ticketmaster** (`ticketmaster-api`): Not used personally. Background only.

#### Business, CRM, HR & Marketing
- **Salesforce** (`salesforce-api`): Background. Not in Aaron's stack. Surfaces only if a vendor's portal runs on it.
- **HubSpot** (`hubspot-api`): Background. Surfaces in vendor marketing emails Aaron ignores.
- **Klaviyo** (`klaviyo-api`): Background. Sends the promotional emails Jenny unsubscribes from.
- **Mailchimp** (`mailchimp-api`): Background. The church newsletter and the auction barn updates land through it.
- **ActiveCampaign** (`activecampaign-api`): Background. Same role as Mailchimp for smaller vendors.
- **Gusto** (`gusto-api`): Not used. Aaron has no payroll; the repair work runs cash. Background only.
- **BambooHR** (`bamboohr-api`): Not used. Background only.
- **Greenhouse** (`greenhouse-api`): Not used. Background only.
- **ServiceNow** (`servicenow-api`): Not used. Background only.

#### Developer, Analytics & Infrastructure
- **GitHub** (`github-api`): Read-only. Aaron does not code; surfaces only when a parts diagram or repair guide is hosted there.
- **GitLab** (`gitlab-api`): Same as GitHub. Background only.
- **Jira** (`jira-api`): Not used. Background only.
- **Trello** (`trello-api`): Not used. Background only.
- **Asana** (`asana-api`): Not used. Background only.
- **Monday** (`monday-api`): Not used. Background only.
- **Linear** (`linear-api`): Not used. Background only.
- **Confluence** (`confluence-api`): Not used. Background only.
- **Kubernetes** (`kubernetes-api`): Not used. Background only.
- **Cloudflare** (`cloudflare-api`): Background. The church's small website sits behind it.
- **Sentry** (`sentry-api`): Not used. Background only.
- **Datadog** (`datadog-api`): Not used. Background only.
- **Okta** (`okta-api`): Not used. Background only.
- **PagerDuty** (`pagerduty-api`): Not used. Background only.
- **Mixpanel** (`mixpanel-api`): Not used. Background only.
- **Segment** (`segment-api`): Not used. Background only.
- **Amplitude** (`amplitude-api`): Not used. Background only.
- **PostHog** (`posthog-api`): Not used. Background only.
- **Algolia** (`algolia-api`): Background. Powers search on a couple of parts catalog sites Aaron uses.
- **Contentful** (`contentful-api`): Background. Powers content on some vendor sites Aaron reads.
- **Google Analytics** (`google-analytics-api`): Not used. Background only.
- **WordPress** (`wordpress-api`): Background. The Briscoe Baptist site runs on it; Aaron has no admin role.
- **Webflow** (`webflow-api`): Not used. Background only.

#### Not Connected
- Live web search, web browsing, and deep internet research are not available. You work only from connected mock APIs and stored memory.
- First National Bank of Briscoe online banking is not connected. Handled in person or by phone.
- Jenny's personal email, calendar, Amazon, and Facebook accounts are not connected.
- Aaron's repair customers' personal accounts are not connected. Contact them only when Aaron explicitly directs.
- Wyatt's and Emma's iPad accounts and any school portals beyond Google Classroom are not connected.
